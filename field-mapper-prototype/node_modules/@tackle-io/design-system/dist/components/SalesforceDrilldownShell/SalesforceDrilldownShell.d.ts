import React from 'react';
import type { SalesforceNavItem } from '../SalesforceShared';
export interface SalesforceDrilldownShellProps {
    /** Navigation items for the Lightning global header subnav. */
    navItems?: SalesforceNavItem[];
    /** Which subnav item is active (matched by label). */
    activeNavItem?: string;
    /** Called when a subnav item is clicked. */
    onNavItemClick?: (label: string) => void;
    /** Breadcrumb trail displayed above the page title. */
    breadcrumbs?: {
        label: string;
        onClick?: () => void;
    }[];
    /** Page title displayed below the breadcrumb. */
    pageTitle: string;
    /** Optional icon rendered to the left of the page title. */
    pageIcon?: React.ReactNode;
    /** Main content (rendered in the primary content column). */
    children?: React.ReactNode;
    /** Quick links rendered in the 220 px sticky sidebar. */
    quickLinks?: {
        label: string;
        href?: string;
        active?: boolean;
    }[];
    /** Action buttons rendered in the fixed sticky footer (e.g. Cancel, Save, Submit). */
    actions?: React.ReactNode;
    /** Additional CSS class on the root element. */
    className?: string;
}
/**
 * SalesforceDrilldownShell -- A Salesforce Lightning Experience drill-down page
 * layout aligned to SLDS 2.x. Renders the full-page chrome including global
 * header, breadcrumb navigation, page title, a flex layout with a main content
 * area and 220 px quick links sidebar, and a fixed sticky action bar.
 *
 * Designed for "Tackle for Salesforce" — Tackle Platform detail pages and form
 * pages rendered inside the Salesforce Canvas chrome (e.g. funding request,
 * MDF form, record detail views).
 */
export declare const SalesforceDrilldownShell: React.FC<SalesforceDrilldownShellProps>;
//# sourceMappingURL=SalesforceDrilldownShell.d.ts.map