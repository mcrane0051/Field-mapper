import React from 'react';
export interface ComboBoxOption {
    /** Unique value for the option */
    value: string;
    /** Display label for the option */
    label: string;
    /** Whether the option is disabled */
    disabled?: boolean;
}
export interface ComboBoxProps {
    /** Label for the combobox */
    label?: string;
    /** Helper text or error message */
    helperText?: string;
    /** Error state */
    error?: boolean;
    /** Available options */
    options?: ComboBoxOption[];
    /** Allow multiple selections */
    multiple?: boolean;
    /** Current value (single select) */
    value?: string;
    /** Current values (multiselect) */
    values?: string[];
    /** Change handler for single select */
    onChange?: (value: string) => void;
    /** Change handler for multiselect */
    onValuesChange?: (values: string[]) => void;
    /** Additional CSS class */
    className?: string;
    /** Whether the field is required */
    required?: boolean;
    /** Placeholder text */
    placeholder?: string;
    /** Whether the field is disabled */
    disabled?: boolean;
    /** Optional id for the input */
    id?: string;
}
/**
 * A selection input that accommodates both single and multiple selection,
 * using typeahead functionality to dynamically search and filter results.
 * Intended to eventually replace Select.
 *
 * Built on Base UI Combobox for robust keyboard navigation,
 * activedescendant management, ARIA compliance, and popup positioning.
 *
 * **Types**
 * - **Single select (Default)** — One value at a time; selected text appears
 *   in the input with SemiBold weight.
 * - **Multiple select** — Multiple values shown as removable `SelectionTag`
 *   chips inside the input. Supports single-row and double-row heights.
 *
 * **States**
 * - **Default** — 1 px Neutral 40 border, white background.
 * - **Hover** — Border promotes to Neutral 60.
 * - **Focus** — 2 px Blue 70 border, label turns Blue 70.
 * - **Error** — 2 px Red 70 border, label turns Red 70, helper becomes error.
 * - **Disabled** — Neutral 20 background, Neutral 40 border, cursor not-allowed.
 *
 * **Accessibility**
 * Implements WAI-ARIA combobox pattern (`role="combobox"`, `aria-expanded`,
 * `aria-controls`, `aria-autocomplete="list"`, `aria-activedescendant`).
 * Full keyboard support: Arrow Up/Down to navigate, Enter to select,
 * Escape to close, Home/End to jump.
 */
export declare const ComboBox: React.ForwardRefExoticComponent<ComboBoxProps & React.RefAttributes<HTMLInputElement>>;
//# sourceMappingURL=ComboBox.d.ts.map