import React from 'react';
export interface DropdownOption {
    /** Unique value for the option */
    value: string;
    /** Display label for the option */
    label: string;
    /** Whether the option is disabled */
    disabled?: boolean;
}
export interface DropdownProps {
    /** Inline label displayed before the selected value (e.g. "Sort:") */
    label: string;
    /** Available options — short, static lists work best */
    options: DropdownOption[];
    /** Currently selected value (always required — there is always a default) */
    value: string;
    /** Callback when the selection changes */
    onChange?: (value: string) => void;
    /** Visual type: "default" or "sort" (adds a sort-direction toggle icon) */
    type?: 'default' | 'sort';
    /** Sort direction — only used when `type="sort"` */
    sortDirection?: 'ascending' | 'descending';
    /** Callback when the sort-direction icon is clicked — only used when `type="sort"` */
    onSortDirectionChange?: (direction: 'ascending' | 'descending') => void;
    /** Whether the dropdown is disabled */
    disabled?: boolean;
    /** Additional CSS class */
    className?: string;
    /** Width for the trigger (defaults to auto) */
    width?: number;
    /** Width for the dropdown menu (defaults to trigger width) */
    menuWidth?: number;
    /** Optional id for the trigger */
    id?: string;
    /** Accessible label override for the trigger */
    'aria-label'?: string;
}
/**
 * A menu-type selection component for short, static lists of actions or options
 * that have an immediate effect on a view or begin a workflow.
 *
 * Built on Base UI Menu for robust keyboard navigation, focus management,
 * automatic positioning, and WAI-ARIA compliance.
 *
 * **Types**
 * - **Default** — Inline label + bold selection + expand caret.
 * - **Sort** — Adds a sort-direction toggle icon (ascending / descending)
 *   before the inline label. Clicking the icon toggles sort direction
 *   without opening the menu.
 *
 * **States**
 * - **Default** — 1 px Neutral 40 border, white background.
 * - **Hover** — Border promotes to Neutral 60.
 * - **Focus** — 2 px Blue 70 border (keyboard `:focus-visible`).
 * - **Disabled** — Neutral 20 background, Neutral 50 text, cursor not-allowed.
 *
 * **Key takeaways**
 * - Typically offers a shorter and static list of menu options.
 * - There is always a default selection; selections are preset and cannot be cleared.
 * - Labels are inline and there is no helper text or error state.
 * - See also: `ComboBox` for typeahead search and multiselect.
 *
 * **Accessibility**
 * Uses Base UI Menu's `aria-haspopup`, `aria-expanded`, and `aria-controls`.
 * Full keyboard navigation (Arrow Up/Down, Enter, Escape, Home/End, typeahead).
 * The sort-direction toggle has its own `aria-label` describing the current
 * direction.
 */
export declare const Dropdown: React.FC<DropdownProps>;
//# sourceMappingURL=Dropdown.d.ts.map