import React from 'react';
export interface ModalAction {
    /** Label for the action button */
    label: string;
    /** Click handler */
    onClick: () => void;
    /** Button variant */
    variant?: 'primary' | 'secondary' | 'destructive';
    /** Whether the button is disabled */
    disabled?: boolean;
}
export interface ModalProps {
    /** Whether the modal is open */
    isOpen: boolean;
    /** Callback when modal should close */
    onClose: () => void;
    /** Modal title */
    title: string;
    /** Optional description/secondary text */
    description?: string;
    /** Modal content */
    children: React.ReactNode;
    /** Modal size */
    size?: 'x-small' | 'small' | 'medium' | 'large';
    /** Whether to show icon in header */
    showIcon?: boolean;
    /** Icon type to display */
    iconType?: 'info' | 'error';
    /** Whether to show the close button in header */
    showCloseButton?: boolean;
    /** Primary action button */
    primaryAction?: ModalAction;
    /** Secondary action button (e.g., Cancel) */
    secondaryAction?: ModalAction;
    /** Whether clicking overlay closes the modal */
    closeOnOverlayClick?: boolean;
    /** Whether pressing Escape closes the modal */
    closeOnEscape?: boolean;
}
/**
 * An overlay box that appears atop the interface and requires an action,
 * dismissal, or user acknowledgement. Modals disable all app functionality
 * to focus the user's attention exclusively on one task.
 *
 * Built on Base UI Dialog for robust focus trapping, scroll locking,
 * focus restoration, and portal rendering.
 *
 * **Sub-types**
 * - **InfoModal** — Informational content; closes via close icon or Escape.
 * - **ActionModal** — Requires a deliberate decision. Variants: Actionable,
 *   Confirmation, Warning, Destructive. Closes via Cancel/Okay buttons or
 *   Escape (which acts as Cancel).
 * - **FormModal** — One-, two-, or three-column form layout.
 *
 * **Sizes** (`size` prop)
 * - `x-small` — 288 px (≈ 20 % of page width)
 * - `small` — 432 px (≈ 30 %)
 * - `medium` — 576 px (≈ 40 %, default)
 * - `large` — 716 px (≈ 50 %)
 * Choose the smallest size that accommodates the content without wrapping
 * the header. If the header wraps at Large, simplify the text.
 *
 * **Behavior**
 * - *Dismissing:* Informational modals use a close icon; actionable modals
 *   use Cancel / Okay buttons. Escape always closes (acts as Cancel for
 *   actionable modals).
 * - *Scrolling:* Modals scroll with the page, not inside their container.
 *   If a long scroll is needed, consider a drawer.
 * - *Nesting:* Do not nest modals.
 *
 * **Overlay**
 * Full-screen scrim (Neutral-50 at 50 % opacity) covering all content
 * including navigation. Modal is centered to the entire page.
 *
 * **Accessibility**
 * Focus is trapped within the modal. Tab/Shift-Tab cycle through focusable
 * elements. Escape closes the modal. Focus returns to the trigger element
 * on close. Rendered with `role="dialog"` and `aria-modal="true"`.
 *
 * @see ModalHeader
 */
export declare const Modal: React.FC<ModalProps>;
//# sourceMappingURL=Modal.d.ts.map