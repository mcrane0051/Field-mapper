import React from 'react';
export interface NavFooterProps extends React.HTMLAttributes<HTMLButtonElement> {
    /** Avatar element (icon, image, or custom component such as an initial block). */
    avatar?: React.ReactNode;
    /** Primary text — user name, account name, or environment name. */
    userName?: string;
    /** Secondary text — email address, "Switch accounts", or "Switch environments". */
    userEmail?: string;
    /**
     * Optional badge rendered between the primary text and secondary text.
     * Typically a `Tag` component (e.g. green "Production" tag for Environment type).
     */
    badge?: React.ReactNode;
    /**
     * Content shown **instead of** the avatar when the footer is collapsed.
     * Used by the Environment type to display a compact Tag (e.g. "Prod")
     * in collapsed state. When omitted the avatar is shown as usual.
     */
    collapsedContent?: React.ReactNode;
    /** Whether the footer is collapsed (showing only avatar or collapsedContent). */
    collapsed?: boolean;
}
/**
 * NavFooter — Primary navigation sidebar footer displaying identity
 * information and serving as the trigger for profile / account / environment
 * menus.
 *
 * ## Types (Figma: Type property)
 * | Type | Avatar | Badge | Collapsed view |
 * |------|--------|-------|----------------|
 * | **User** | UserCircled icon (circular, Neutral 40, 32 × 32) | — | Avatar only |
 * | **Account** | UserCircled icon (circular, Neutral 40, 32 × 32) | — | Avatar only |
 * | **Environment** | Square initial avatar (32 × 32, Neutral 50 bg, 4 px radius) | Tag (e.g. green "Production") | Compact Tag (e.g. "Prod") via `collapsedContent` |
 *
 * ## Layout
 * - Expanded: 240 px wide, horizontal flex, 12 px gap,
 *   padding `16 16 16 20`.
 * - Collapsed: 64 px wide, centered, padding `20 16`.
 *
 * ## States
 * - **Default** — Teal 90 background, 1 px top border (Teal 100).
 * - **Hover** — Entire footer darkens to Teal 100.
 *
 * ## Typography
 * - Primary text (userName): Open Sans SemiBold 12 / 20, White.
 * - Secondary text (userEmail): Open Sans Regular 12 / 20, Neutral 40.
 *
 * ## Collapsed behavior
 * When `collapsed` is `true` the text block is hidden.
 * - For User / Account: Only the 32 × 32 avatar is shown.
 * - For Environment: If `collapsedContent` is provided it replaces the
 *   avatar (typically a compact Tag like "Prod").
 *
 * ## Avatar
 * Accepts any `ReactNode` via `avatar`. When omitted the `UserCircledIcon`
 * (Neutral 40, 32 × 32) is used. The container does **not** force a shape —
 * circular or square styling is determined by the avatar element itself.
 *
 * ## Accessibility
 * Renders as `<button type="button">` — keyboard-focusable and activatable
 * with Enter / Space. Focus ring uses `:focus-visible` only.
 *
 * @remarks
 * Place as the last child(ren) in a vertical navigation sidebar, directly
 * below `NavMenuItem` items. Pairs with `NavHeader` at the top. Up to three
 * NavFooter instances stack at the bottom of the sidebar: Account, Environment,
 * and User.
 *
 * @see Figma — NavFooter ComponentSet (node 2701:11906)
 */
export declare const NavFooter: React.ForwardRefExoticComponent<NavFooterProps & React.RefAttributes<HTMLButtonElement>>;
//# sourceMappingURL=NavFooter.d.ts.map