import React, { ButtonHTMLAttributes } from 'react';
export interface NavMenuItemProps extends Omit<ButtonHTMLAttributes<HTMLButtonElement>, 'children'> {
    /**
     * The icon element (24 × 24 recommended).
     * @example <DashboardIcon /> or <SettingsIcon />
     */
    icon: React.ReactNode;
    /** The label text (hidden when collapsed) */
    label: string;
    /** Whether this item is the active/current page */
    active?: boolean;
    /** Whether the menu is collapsed (icon only) */
    collapsed?: boolean;
    /** Render as a horizontal divider instead of a navigation item */
    divider?: boolean;
    /** Click handler for navigation */
    onClick?: () => void;
    /** Additional CSS class */
    className?: string;
}
/**
 * Primary sidebar navigation item with icon and optional label.
 *
 * **States**
 * - **Default** — Teal 90 background, icon in Neutral 20, label in white.
 * - **Hover** — Background darkens to Teal 100 (non-active items only).
 * - **Active** — Teal 100 background, 4 px Yellow 50 left indicator (inside-aligned),
 *   bold label, icon promoted to white.
 *
 * **Collapsed behavior**
 * When `collapsed` is `true` the label is hidden and only the 24 × 24 icon is
 * shown, centered within a 64 px wide container.
 *
 * **Divider variant**
 * When `divider` is `true` the component renders as a non-interactive horizontal
 * line separator (Teal 100) with 12 px vertical padding. Icon and label are
 * ignored.
 *
 * **Accessibility**
 * Renders as a `<button type="button">` with `aria-label` set to the label text
 * and `aria-current="page"` when active. Focus ring uses `:focus-visible` only.
 * Dividers render as an inert `<div>` with `role="separator"`.
 *
 * @remarks
 * Used inside a vertical navigation sidebar between `NavHeader` (top) and
 * `NavFooter` (bottom). Supports 13 domain-specific icon/label pairings in the
 * Tackle Design Library.
 */
export declare const NavMenuItem: React.ForwardRefExoticComponent<NavMenuItemProps & React.RefAttributes<HTMLButtonElement>>;
//# sourceMappingURL=NavMenuItem.d.ts.map