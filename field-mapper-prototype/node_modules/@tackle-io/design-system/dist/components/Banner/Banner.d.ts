import React from 'react';
import './Banner.css';
export interface BannerProps {
    /** Visual variant of the banner */
    variant?: 'info' | 'success' | 'warning' | 'error';
    /** Position of the colored border */
    borderPosition?: 'top' | 'left';
    /** Whether the banner can be expanded/collapsed */
    expandable?: boolean;
    /** Initial expanded state (only used if expandable is true) */
    defaultExpanded?: boolean;
    /** Banner title text */
    title: string;
    /** Optional body content (shown when expanded) */
    children?: React.ReactNode;
    /** Optional custom icon to override default */
    icon?: React.ReactNode;
    /** Additional CSS class names */
    className?: string;
    /** Callback when banner is clicked (for expandable banners) */
    onClick?: () => void;
}
/**
 * Banner — a static container used to communicate important conditions or
 * information within the context of a page. See also: Alert.
 *
 * @remarks
 * Banners are embedded at the top of a page or section and should generally
 * match the width of other page content. In some cases they may be expanded
 * to reveal additional information.
 *
 * **Types** control color and semantic intent:
 * - `info` — Informational (Blue 70).
 * - `success` — Positive confirmation (Green 70).
 * - `warning` — Caution (Yellow 40).
 * - `error` — Danger / failure (Red 70).
 *
 * **Variations:**
 * - Static (non-expandable) — Persistent message, no interaction.
 * - Expandable — Collapsed by default; click to reveal body content.
 * - Border position — Top or Left accent border.
 *
 * **States** (expandable variant only):
 * - Default, Hover, Focus.
 */
export declare const Banner: React.FC<BannerProps>;
//# sourceMappingURL=Banner.d.ts.map