import React from 'react';
export interface MenuItem {
    value: string;
    label: string;
    disabled?: boolean;
}
export interface MenuProps extends Omit<React.HTMLAttributes<HTMLDivElement>, 'onSelect'> {
    /** Array of menu items */
    items: MenuItem[];
    /** Currently selected value(s) */
    selectedValue?: string;
    /** Callback when an item is selected */
    onSelect?: (value: string) => void;
    /** Density mode: 'action' for 16px padding, 'data' for 8px padding */
    density?: 'action' | 'data';
    /** Width of the menu */
    width?: number;
    /** Accessible label for the menu */
    'aria-label'?: string;
    /** ID of the element that labels this menu */
    'aria-labelledby'?: string;
}
/**
 * A rendered menu list for displaying selectable options. Used as a
 * standalone display component — for the full popup-triggered pattern
 * with automatic positioning and focus management, use `Dropdown` instead
 * (which is built on Base UI Menu).
 *
 * **Types** (per item)
 * - **Default** — Standard menu item.
 * - **Selected** — Shows a check icon to indicate the current selection.
 *
 * **Density**
 * - **Action (16 px)** — More spacious; used when triggered by `Dropdown`
 *   or `Button` for action-based workflows.
 * - **Data (8 px)** — Denser; used when triggered by `ComboBox` for
 *   making selections in form contexts.
 *
 * **Accessibility**
 * Renders with `role="menu"`. Full keyboard navigation: Arrow Up/Down,
 * Home, End, Enter/Space to select, Escape to dismiss, and typeahead
 * character search per the WAI-ARIA menu pattern.
 *
 * See also: `Dropdown`, `Button`, `ComboBox`.
 */
declare const Menu: React.ForwardRefExoticComponent<MenuProps & React.RefAttributes<HTMLDivElement>>;
export default Menu;
//# sourceMappingURL=Menu.d.ts.map