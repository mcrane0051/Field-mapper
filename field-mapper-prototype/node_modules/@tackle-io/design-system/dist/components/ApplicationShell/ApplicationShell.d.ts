import React from 'react';
import { NavigationItem } from '../Navigation/Navigation';
export interface ApplicationShellProps {
    /** Content rendered inside the main area (to the right of the sidebar). */
    children?: React.ReactNode;
    /** ID of the currently active navigation item. */
    activeItemId?: string;
    /** Whether the sidebar is collapsed (64 px) or expanded (240 px). */
    collapsed?: boolean;
    /** Callback when the sidebar collapse toggle is clicked. */
    onToggle?: () => void;
    /** Callback when a nav item is clicked, receiving the item ID. */
    onNavigate?: (itemId: string) => void;
    /** Custom navigation items. Defaults to the canonical Tackle nav order. */
    items?: NavigationItem[];
    /** Account name shown in the sidebar account-switcher footer. */
    accountName?: string;
    /** Label beneath the account name (e.g. "Switch accounts"). */
    accountLabel?: string;
    /** Account avatar element. */
    accountAvatar?: React.ReactNode;
    /** Callback when the account footer is clicked. */
    onAccountClick?: () => void;
    /** Environment name shown in the sidebar environment-switcher footer. */
    environmentName?: string;
    /** Label beneath the environment name (e.g. "Switch environments"). */
    environmentLabel?: string;
    /** Environment avatar element (e.g. a square initial block). */
    environmentAvatar?: React.ReactNode;
    /** Environment badge (e.g. a green Tag "Production"), shown between name and label. */
    environmentBadge?: React.ReactNode;
    /** Compact content shown instead of the avatar when the sidebar is collapsed (e.g. a short Tag "Prod"). Defaults to environmentBadge. */
    environmentCollapsedContent?: React.ReactNode;
    /** Callback when the environment footer is clicked. */
    onEnvironmentClick?: () => void;
    /** User display name in the sidebar user footer. */
    userName?: string;
    /** User email in the sidebar user footer. */
    userEmail?: string;
    /** User avatar element. */
    userAvatar?: React.ReactNode;
    /** Callback when the user footer is clicked. */
    onUserClick?: () => void;
    /** Additional CSS class applied to the shell root. */
    className?: string;
}
/**
 * ApplicationShell — The top-level page layout for the Tackle Platform.
 *
 * Composes the `Navigation` sidebar on the left with a scrollable main
 * content area on the right. All Navigation props (items, active state,
 * collapse, footer sections) are forwarded through.
 *
 * ## Layout
 * - **Root**: Full viewport (`100vw × 100vh`), horizontal flex, hidden overflow.
 * - **Sidebar**: `Navigation` component — 240 px expanded, 64 px collapsed.
 *   Sticky full-height, Teal 90 background.
 * - **Main content area**: Fills remaining width, vertical scroll, Neutral 10
 *   background. Accepts any `children` — typically a `PageTitle` followed by
 *   page-specific content.
 *
 * ## Content area (Page body > Content)
 * The `.pageContent` region applies 32 px padding from the main edges. Inside
 * it, `.pageContentInner` wraps all `children` with **max width 1216 px**
 * (centered when the main area is wider), a column flex layout, and 32 px
 * gap between direct children — matching the Figma "Page body > Content" frame.
 *
 * ## Sidebar anatomy (via Navigation)
 * - **NavHeader** — Tackle logo + collapse toggle.
 * - **NavList** — Vertical stack of `NavMenuItem` items with divider separators.
 * - **Footer** — Up to three `NavFooter` sections: Account switcher,
 *   Environment switcher, User information.
 *
 * ## Accessibility
 * - The sidebar renders inside `<nav aria-label="Primary navigation">`.
 * - The main content area uses `<main>` landmark.
 * - Active nav items have `aria-current="page"`.
 * - Collapse toggle has `aria-label` and `aria-expanded`.
 *
 * @see Figma — Application Shell ComponentSet (node 2836:2795)
 */
export declare const ApplicationShell: React.FC<ApplicationShellProps>;
//# sourceMappingURL=ApplicationShell.d.ts.map