import React from 'react';
export interface PageTitleAction {
    /** Label for the action button */
    label: string;
    /** Click handler for the action */
    onClick: () => void;
    /** Whether the button is disabled */
    disabled?: boolean;
}
export interface PageTitleProps {
    /** The main page title/heading */
    title: string;
    /** Optional description text below the title */
    description?: string;
    /** Whether to show the sync information */
    showSync?: boolean;
    /** Last sync date/time string (e.g., "12/03/2024 at 14:30:00") */
    lastSyncDate?: string;
    /** Callback when "Sync with Salesforce now" is clicked */
    onSyncClick?: () => void;
    /** Whether to show the settings button */
    showSettings?: boolean;
    /** Callback when settings button is clicked */
    onSettingsClick?: () => void;
    /** Whether to show the filter button */
    showFilter?: boolean;
    /** Callback when filter button is clicked */
    onFilterClick?: () => void;
    /** Whether to show the download button */
    showDownload?: boolean;
    /** Callback when download button is clicked */
    onDownloadClick?: () => void;
    /** Whether to show the date picker ComboBox */
    showDatePicker?: boolean;
    /** Current date picker value */
    datePickerValue?: string;
    /** Available date picker options */
    datePickerOptions?: Array<{
        value: string;
        label: string;
    }>;
    /** Callback when date picker changes */
    onDatePickerChange?: (value: string) => void;
    /** Text for the date picker link (e.g., "Set fiscal year") */
    datePickerLinkText?: string;
    /** Callback when date picker link is clicked */
    onDatePickerLinkClick?: () => void;
    /** Primary action button configuration */
    primaryAction?: PageTitleAction;
    /** Secondary action button configuration */
    secondaryAction?: PageTitleAction;
    /** Additional CSS class */
    className?: string;
}
/**
 * Page-level header combining a hero title, optional description,
 * optional Salesforce sync status, and a right-aligned toolbar.
 *
 * **Layout**
 * Horizontal split: Headline (left, flexible) and Toolbar (right, fixed).
 * The headline stacks title → description → sync info vertically.
 * The toolbar stacks a button row → optional date picker link vertically.
 *
 * **Title**
 * Open Sans Light 40 px / 60 px line-height, Neutral 100.
 *
 * **Description**
 * Open Sans Regular 16 px / 24 px, Neutral 100. Appears directly below
 * the title with no additional spacing.
 *
 * **Sync Information**
 * Displayed when `showSync` is true. Shows a "Last sync" sentence followed
 * by a Reset icon + "Sync with Salesforce now" link in Blue 70.
 *
 * **Toolbar** (right side, 8 px gap between items)
 * Ordered left-to-right: icon buttons (Settings, Filter, Download) →
 * date picker ComboBox → Secondary CTA → Primary CTA.
 * Below the button row, an optional date picker link (SemiBold 12 px,
 * Blue 70) is right-aligned.
 *
 * **Accessibility**
 * Title renders as `<h1>`. Icon buttons include `aria-label`. The sync
 * link and date picker link are `<button>` elements for keyboard access.
 */
export declare const PageTitle: React.FC<PageTitleProps>;
//# sourceMappingURL=PageTitle.d.ts.map