import React from 'react';
import './HelperText.css';
export interface HelperTextProps {
    /** The helper text content to display. */
    children?: React.ReactNode;
    /**
     * Type of helper text.
     * - `guidance` — Neutral 70 guidance message below a field.
     * - `error` — Red 70 error message with a warning icon.
     * - `counter` — Character count (e.g. "45/100"), Neutral 70.
     * - `none` — Hidden spacer (preserves vertical alignment when no helper text is needed).
     * @default 'guidance'
     */
    type?: 'guidance' | 'error' | 'counter' | 'none';
    /** Optional `id` for `aria-describedby` or `aria-errormessage` association. */
    id?: string;
}
/**
 * HelperText — messaging variants used within form field and control group
 * components.
 *
 * @remarks
 * **Types:**
 * - `guidance` — Neutral 70 helper text below a field.
 * - `error` — Red 70 message preceded by a warning triangle icon.
 * - `counter` — Character count display (e.g. "45/100").
 * - `none` — Empty spacer preserving vertical layout when no message is needed.
 *
 * Pass an `id` so parent controls can reference this element via
 * `aria-describedby` (guidance/counter) or `aria-errormessage` (error).
 */
export declare const HelperText: React.FC<HelperTextProps>;
//# sourceMappingURL=HelperText.d.ts.map