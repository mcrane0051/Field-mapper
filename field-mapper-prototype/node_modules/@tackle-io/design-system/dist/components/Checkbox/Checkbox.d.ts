import React, { InputHTMLAttributes } from 'react';
import './Checkbox.css';
export interface CheckboxProps extends Omit<InputHTMLAttributes<HTMLInputElement>, 'type'> {
    /** Label text to display next to the checkbox. */
    label?: string;
    /** Whether the checkbox is in an indeterminate (partial-selection) state. */
    indeterminate?: boolean;
    /** Additional CSS class names. */
    className?: string;
}
/**
 * Checkbox — an input control used to select one or more predefined options,
 * which are applied via a separate submit action.
 *
 * @remarks
 * **Types:**
 * - `Default` — Standard checked/unchecked toggle.
 * - `Indeterminate` — Partial selection, typically for "select all" parents.
 *
 * **States:** Default, Hover, Focus, Disabled.
 *
 * **Variations:**
 * - `Filled` (Yes/No) — Whether the checkbox is checked or unchecked.
 * - `Labeled` (Yes/No) — With or without an adjacent text label.
 *
 * See also: Radio, Switch.
 */
export declare const Checkbox: React.ForwardRefExoticComponent<CheckboxProps & React.RefAttributes<HTMLInputElement>>;
//# sourceMappingURL=Checkbox.d.ts.map