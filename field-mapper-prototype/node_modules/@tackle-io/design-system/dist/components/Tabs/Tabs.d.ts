import React, { ReactNode } from 'react';
export interface TabItem {
    /** Unique identifier for the tab */
    id: string;
    /** Label text for the tab */
    label: string;
    /** Optional icon element */
    icon?: ReactNode;
    /** Optional badge count */
    count?: number;
    /** Whether the tab is disabled */
    disabled?: boolean;
    /** Whether the tab is in an error state (Red 70 text) */
    error?: boolean;
}
export interface TabsProps {
    /** Array of tab items */
    items: TabItem[];
    /** Currently active tab id */
    activeTab?: string;
    /** Callback when tab is clicked */
    onTabChange?: (tabId: string) => void;
    /** Visual variant of the tabs */
    variant?: 'platform' | 'card' | 'navigation';
    /** Additional CSS class */
    className?: string;
    /** Optional tab panel content keyed by tab id */
    panels?: Record<string, ReactNode>;
}
/**
 * Tabs provide segmented navigation within a content area. Built on
 * Base UI Tabs for full WAI-ARIA compliance (arrow-key navigation,
 * roving tabindex, automatic tab-to-panel association).
 *
 * **Variants**
 * - **Platform Tabs** — Full-width, used at the page level for top-level
 *   content sections. SemiBold 14 px text, Blue 70 underline indicator,
 *   and a 1 px bottom rule spanning the tab group width.
 * - **Tackle Card Tabs** — Compact tabs within cards or smaller containers.
 *   Regular/Bold 12 px text, Blue 60 underline indicator, optional count
 *   badge appended to label text (e.g. "AWS (1)").
 * - **Tackle Card Navigation Tabs** — Tabs with optional leading icons for
 *   section-level navigation inside cards. 12 px text, selected state
 *   switches to white background with no bottom rule.
 *
 * **States**
 * - Default (unselected) — Neutral text, subtle bottom border.
 * - Hover — Slightly emphasized text (variant-specific).
 * - Selected — Accent color indicator, bold text (Card/Navigation),
 *   accent text (Platform).
 * - Error — Red 70 text with a leading triangle warning icon;
 *   set `error: true` on the `TabItem`. Replaces the domain icon
 *   in Navigation tabs.
 * - Disabled — Neutral 50 text, `cursor: not-allowed`.
 * - Focus — 2 px Blue 70 focus ring (keyboard only).
 *
 * **Sizing**
 * - Platform: 40 px height, 12/16 px padding.
 * - Card: 40 px height, 10/12 px padding.
 * - Navigation: 44 px height, 12/16 px padding.
 *
 * **Accessibility**
 * `role="tablist"` on the container, `role="tab"` on each item,
 * `role="tabpanel"` on each panel. Arrow keys move focus; the
 * selected tab receives `aria-selected="true"`.
 */
export declare const Tabs: React.FC<TabsProps>;
//# sourceMappingURL=Tabs.d.ts.map