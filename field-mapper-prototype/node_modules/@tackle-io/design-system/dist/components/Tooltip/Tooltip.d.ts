import React from 'react';
export type TooltipPosition = 'top-start' | 'top' | 'top-end' | 'bottom-start' | 'bottom' | 'bottom-end' | 'left-start' | 'left' | 'left-end' | 'right-start' | 'right' | 'right-end';
export interface TooltipProps {
    /** Body content of the tooltip. */
    content: React.ReactNode;
    /** Optional bold header text above the body. */
    header?: string;
    /** Optional footer link at the bottom of the tooltip. */
    footer?: {
        label: string;
        href?: string;
        onClick?: () => void;
    };
    /** Placement relative to the trigger element. */
    position?: TooltipPosition;
    /** Whether to show the directional arrow. Defaults to `true`. */
    showArrow?: boolean;
    /** The trigger element that activates the tooltip on hover. */
    children: React.ReactElement;
}
/**
 * Provides description or additional information/context about a UI element.
 * Tooltips can be used both simply (body only) and robustly (with header
 * and footer link).
 *
 * Built on Base UI Tooltip for robust floating positioning,
 * smart delay management, and automatic viewport detection.
 *
 * **Variants**
 * - Default — Body text only.
 * - With header — Bold title above the body.
 * - With footer — Link below the body.
 * - With header and footer — Both.
 *
 * **Position** (12 placements + no-arrow)
 * `top-start`, `top`, `top-end`, `bottom-start`, `bottom`, `bottom-end`,
 * `left-start`, `left`, `left-end`, `right-start`, `right`, `right-end`.
 *
 * **Sizing**
 * - Min: 39 px wide, 44 px tall (one line).
 * - Max: 480 px wide, 212 px tall (header + 7 lines + footer).
 *
 * **Interaction**
 * Triggered on hover; disappears on mouse leave. The tooltip is placed
 * 8 px from the trigger element. Footer links are interactive.
 *
 * **Arrow**
 * 16 x 8 px CSS triangle rendered as a pseudo-element on the Popup div.
 * Positioned via Base UI's `data-side` / `data-align` attributes so it
 * tracks collision-avoidance flips automatically.
 * Set `showArrow={false}` to hide.
 *
 * **Accessibility**
 * Uses `role="tooltip"` with `aria-describedby` linking the trigger to
 * the tooltip content. The arrow is purely decorative (CSS pseudo-element,
 * no DOM node, no ARIA impact).
 *
 * @see DocsTooltip (planned) — for documentation-style tooltips with richer content.
 */
export declare const Tooltip: React.FC<TooltipProps>;
//# sourceMappingURL=Tooltip.d.ts.map