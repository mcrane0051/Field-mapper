import React from 'react';
import type { SalesforceNavItem } from '../SalesforceShared';
export interface SalesforceOpportunityShellProps {
    /** Navigation items for the Lightning global header subnav. */
    navItems?: SalesforceNavItem[];
    /** Which subnav item is active (matched by label). */
    activeNavItem?: string;
    /** Called when a subnav item is clicked. */
    onNavItemClick?: (label: string) => void;
    /** Opportunity record name displayed in the record header. */
    opportunityName: string;
    /** Record type label above the name (default "Opportunity"). */
    recordType?: string;
    /** Action buttons rendered to the right of the record header (e.g. "+ Follow", "New Case"). */
    headerActions?: React.ReactNode;
    /** Key-value pairs displayed in the highlights panel below the header. */
    highlights?: {
        label: string;
        value: string;
    }[];
    /** Sales path stages rendered as a stage-gate progress indicator. */
    pathStages?: {
        label: string;
        state: 'done' | 'active' | 'upcoming';
    }[];
    /** Called when the "Mark Stage as Complete" button is clicked. */
    onMarkStageComplete?: () => void;
    /** Tab definitions for the main column tabbed area. */
    tabs?: {
        id: string;
        label: string;
    }[];
    /** Currently active tab id. */
    activeTab?: string;
    /** Called when a tab is clicked. */
    onTabChange?: (tabId: string) => void;
    /** Content rendered inside the active tab panel. */
    children?: React.ReactNode;
    /** Content rendered in the 320px right sidebar column (typically TackleCard). */
    sidebarContent?: React.ReactNode;
    /** Items rendered in the fixed utility bar at the bottom. */
    utilityBarItems?: {
        label: string;
        onClick?: () => void;
    }[];
    /** Additional CSS class on the root element. */
    className?: string;
}
/**
 * SalesforceOpportunityShell -- A Salesforce Lightning Experience record page
 * layout aligned to SLDS 2.x. Renders the full-page chrome including global
 * header, opportunity record header, highlights, sales path, tabbed content
 * area with a 320 px right sidebar slot (for TackleCard), and a fixed utility
 * bar.
 *
 * Designed for "Tackle for Salesforce" — Tackle UI rendered inside a
 * Salesforce Canvas app on the Opportunity record page.
 */
export declare const SalesforceOpportunityShell: React.FC<SalesforceOpportunityShellProps>;
//# sourceMappingURL=SalesforceOpportunityShell.d.ts.map