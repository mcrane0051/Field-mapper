import React from 'react';
import './GroupLabel.css';
export interface GroupLabelProps {
    /** The group label text to display. */
    children: React.ReactNode;
    /**
     * Visual state of the label.
     * - `default` — Neutral 100 text.
     * - `error` — Red 70 text (label and asterisk).
     * - `none` — Hidden spacer (preserves vertical alignment when no label is needed).
     * @default 'default'
     */
    state?: 'default' | 'error' | 'none';
    /**
     * Whether the associated group is required.
     * When true, an asterisk (*) is rendered after the label text.
     * @default false
     */
    required?: boolean;
    /** HTML `id` attribute for the group label element. */
    id?: string;
}
/**
 * GroupLabel — label variants used within control group components
 * (Radio, Checkbox, Switch).
 *
 * @remarks
 * **States:**
 * - `default` — Neutral 100 text.
 * - `error` — Red 70 text; asterisk also Red 70.
 * - `none` — Empty spacer (preserves vertical alignment when no label is needed).
 *
 * **Required:** Appends an asterisk (*) after the label text. The asterisk
 * is always Red 70 unless in error state, where both label and asterisk
 * share the same Red 70 color.
 *
 * Unlike {@link FormLabel}, GroupLabel has no focus state and uses Neutral 100
 * (darker) as the default text color.
 */
export declare const GroupLabel: React.FC<GroupLabelProps>;
//# sourceMappingURL=GroupLabel.d.ts.map