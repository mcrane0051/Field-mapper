import React from 'react';
export interface NavHeaderProps extends React.HTMLAttributes<HTMLDivElement> {
    /** Logo content to display (text, image, or custom component). Hidden when collapsed. */
    logo?: React.ReactNode;
    /**
     * Whether the navigation is collapsed.
     * - `false` — 240px wide, 64px tall, shows TackleMenu icon + logo.
     * - `true` — 64px wide, 64px tall, shows only TackleMenu icon.
     * @default false
     */
    collapsed?: boolean;
    /** Callback fired when the expand/collapse toggle button is clicked. */
    onToggle?: () => void;
}
/**
 * NavHeader — primary navbar header with logo and expand/collapse functionality.
 *
 * @remarks
 * **Collapsed states:**
 * - Expanded (240px wide) — TackleMenu icon + logo; header row **64px** tall (Figma PrimaryNav).
 * - Collapsed (64px wide) — TackleMenu icon only, centered in a **64px** square header.
 *
 * **Interaction:**
 * - Hover darkens the entire header from Teal 90 to Teal 100.
 * - Toggle button has `aria-label` and `aria-expanded` for screen readers.
 *
 * Used at the top of the PrimaryNav sidebar.
 */
export declare const NavHeader: React.ForwardRefExoticComponent<NavHeaderProps & React.RefAttributes<HTMLDivElement>>;
//# sourceMappingURL=NavHeader.d.ts.map