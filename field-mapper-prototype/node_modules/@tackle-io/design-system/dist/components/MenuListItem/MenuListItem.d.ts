import React, { ButtonHTMLAttributes } from 'react';
export interface MenuListItemProps extends Omit<ButtonHTMLAttributes<HTMLButtonElement>, 'type'> {
    /** The text content of the list item. */
    children?: React.ReactNode;
    /** Whether the item is currently selected (shows a check icon). */
    selected?: boolean;
    /** Whether the item is disabled (Neutral 50 text, no interaction). */
    disabled?: boolean;
    /**
     * Density variant controlling vertical padding.
     * - `data` — 4px vertical padding (compact, 30px total height).
     * - `action` — 8px vertical padding (spacious, 38px total height).
     * @default 'data'
     */
    density?: 'data' | 'action';
    /** Click handler. */
    onClick?: () => void;
    /** Additional CSS class. */
    className?: string;
}
/**
 * MenuListItem — selectable list item variants used within the Menu component.
 *
 * @remarks
 * **Types:**
 * - `Default` — Standard unselected item.
 * - `Selected` — SemiBold text with a Blue 50 check icon.
 *
 * **Density:**
 * - `data` (8px total vertical) — Compact for data-heavy menus.
 * - `action` (16px total vertical) — Spacious for action menus.
 *
 * **States:**
 * - Default — Transparent background, Neutral 90 text.
 * - Hover — Blue 10 background.
 * - Focus — 2px inside border, Blue 70 (keyboard only).
 * - Disabled — Neutral 50 text, no opacity change.
 *
 * Uses `role="menuitem"` and `aria-selected` for accessibility.
 */
export declare const MenuListItem: React.ForwardRefExoticComponent<MenuListItemProps & React.RefAttributes<HTMLButtonElement>>;
//# sourceMappingURL=MenuListItem.d.ts.map