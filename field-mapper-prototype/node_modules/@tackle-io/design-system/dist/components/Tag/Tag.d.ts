import React from 'react';
export type TagColor = 'blue' | 'red' | 'purple' | 'green' | 'gray' | 'yellow';
export interface TagProps {
    /** The text content to display in the tag */
    children: React.ReactNode;
    /** The color variant of the tag */
    color?: TagColor;
    /** Additional CSS class name */
    className?: string;
}
/**
 * Tag — a compact, non-interactive label used to categorize items or
 * display status at a glance.
 *
 * @remarks
 * **Colors** communicate semantic meaning:
 * - `blue` — Informational / default (e.g., Submitted).
 * - `red` — Error or urgent (e.g., Rejected).
 * - `purple` — Categorical (e.g., Feature).
 * - `green` — Success (e.g., Approved).
 * - `gray` — Neutral / inactive (e.g., Pending).
 * - `yellow` — Warning / in-progress (e.g., In Progress).
 *
 * Tags are read-only indicators — they have no interactive states. For a
 * selectable label with remove/toggle behavior, see SelectionTag.
 */
export declare const Tag: React.FC<TagProps>;
//# sourceMappingURL=Tag.d.ts.map