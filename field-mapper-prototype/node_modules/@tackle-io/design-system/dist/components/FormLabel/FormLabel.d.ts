import React from 'react';
import './FormLabel.css';
export interface FormLabelProps {
    /** The label text to display. */
    children: React.ReactNode;
    /**
     * Visual state of the label.
     * - `default` — Neutral text color.
     * - `focus` — Blue text (label and asterisk).
     * - `error` — Red text; bold when required.
     * - `none` — Hidden spacer (no visible label or asterisk).
     * @default 'default'
     */
    state?: 'default' | 'focus' | 'error' | 'none';
    /**
     * Whether the associated field is required.
     * When true, an asterisk (*) is rendered after the label text.
     * @default false
     */
    required?: boolean;
    /** HTML `for` attribute — connects the label to an input by id. */
    htmlFor?: string;
}
/**
 * FormLabel — label variants used within form field components
 * (ComboBox and TextField).
 *
 * @remarks
 * **States:**
 * - `default` — Neutral 70 text.
 * - `focus` — Blue 70 text; asterisk also turns Blue 70.
 * - `error` — Red 70 text; when required, weight becomes Bold (700).
 * - `none` — Empty spacer (preserves vertical alignment when no label is needed).
 *
 * **Required:** Appends an asterisk (*) after the label text. The asterisk
 * color follows the current state (red by default, blue on focus, red on error).
 */
export declare const FormLabel: React.FC<FormLabelProps>;
//# sourceMappingURL=FormLabel.d.ts.map