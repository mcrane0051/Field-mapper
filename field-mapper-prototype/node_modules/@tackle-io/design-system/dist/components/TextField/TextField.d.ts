import React from 'react';
export interface TextFieldProps extends Omit<React.InputHTMLAttributes<HTMLInputElement | HTMLTextAreaElement>, 'size' | 'type'> {
    /** Label text for the input */
    label?: string;
    /** Helper text displayed below the input */
    helperText?: string;
    /** Error message to display (enables error state) */
    error?: string;
    /** Type of input */
    type?: 'text' | 'number' | 'email' | 'password' | 'url' | 'tel';
    /** Whether to render as a multiline textarea */
    multiline?: boolean;
    /** Number of rows for multiline (default: 5) */
    rows?: number;
    /** Icon to display on the left side */
    leftIcon?: React.ReactNode;
    /** Icon to display on the right side */
    rightIcon?: React.ReactNode;
    /** Character counter: shows current/max characters */
    maxLength?: number;
    /** Show character counter even without maxLength */
    showCharacterCount?: boolean;
}
/**
 * An input field accommodating various text formats including single and
 * multi-line text, search inputs, color hex values, and numbers.
 *
 * **Types** (via props)
 * - **Default** — Standard single-line text input.
 * - **Multiline** — `multiline` prop renders a `<textarea>`.
 * - **Number** — `type="number"`.
 * - **Left icon** — `leftIcon` prop places an icon before the input.
 * - **Right icon** — `rightIcon` prop places an icon after the input
 *   (e.g., password visibility toggle).
 * - **Color picker** — `type="color"` (or custom implementation).
 *
 * **States**
 * Default, Hover, Focus, Error, Disabled.
 *
 * **Variations**
 * - Optional / Required — via `required` prop (FormLabel shows asterisk).
 * - Left icon with no label — e.g., Search field.
 * - Right icon with obfuscation — e.g., password toggle.
 * - Number prefix — use a left icon or label to denote a prefix.
 * - Character counter — via `maxLength` / `showCharacterCount` props
 *   (multiline only).
 * - Helper text: None / Guidance / Error / Character counter.
 *
 * **Accessibility**
 * Uses `aria-invalid`, `aria-describedby`, and `aria-errormessage` for
 * error states. Label is linked to the input via `htmlFor` / `id`.
 *
 * @see FormLabel
 * @see HelperText
 */
export declare const TextField: React.ForwardRefExoticComponent<TextFieldProps & React.RefAttributes<HTMLInputElement | HTMLTextAreaElement>>;
//# sourceMappingURL=TextField.d.ts.map