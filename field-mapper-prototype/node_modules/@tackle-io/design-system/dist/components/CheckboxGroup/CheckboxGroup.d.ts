import React from 'react';
export interface CheckboxGroupProps {
    /** Group label text. */
    label?: string;
    /** Helper text displayed below the group. */
    helperText?: string;
    /** Error message — activates error state on group label and helper text. */
    error?: string;
    /** Layout direction. */
    direction?: 'vertical' | 'horizontal';
    /** Number of columns (for grid layout). */
    columns?: 1 | 2 | 3;
    /** Whether the group is required. */
    required?: boolean;
    /** Whether all checkboxes in the group are disabled. */
    disabled?: boolean;
    /** Children checkbox components. */
    children: React.ReactNode;
    /** Additional className. */
    className?: string;
}
/**
 * A group of checkboxes forming a list of controls. A group header
 * or helper text may be included for additional user guidance.
 *
 * Wraps checkboxes in a `<fieldset>` with `role="group"`. Uses `<legend>`
 * for the group label and `aria-describedby` for helper/error text.
 *
 * @see Checkbox
 * @see GroupLabel
 * @see HelperText
 */
export declare const CheckboxGroup: React.FC<CheckboxGroupProps>;
//# sourceMappingURL=CheckboxGroup.d.ts.map