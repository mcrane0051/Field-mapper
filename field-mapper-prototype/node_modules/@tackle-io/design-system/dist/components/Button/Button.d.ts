import React from 'react';
import './Button.css';
export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
    /**
     * Button variant type
     * @default 'primary'
     */
    variant?: 'primary' | 'secondary' | 'destructive' | 'menu';
    /**
     * Button size
     * @default 'standard'
     */
    size?: 'standard' | 'small';
    /**
     * Icon to display on the left side of the button (or as the only content if iconOnly is true)
     */
    leftIcon?: React.ReactNode;
    /**
     * Icon to display on the right side of the button
     */
    rightIcon?: React.ReactNode;
    /**
     * Whether the button should only display an icon without text
     * @default false
     */
    iconOnly?: boolean;
    /**
     * Whether the button is in a loading state
     * @default false
     */
    loading?: boolean;
    /**
     * Button content (optional when iconOnly is true)
     */
    children?: React.ReactNode;
}
/**
 * Button — the primary element users interact with to complete core actions
 * such as initiating or canceling processes, creating and deleting assets,
 * and saving or submitting data.
 *
 * @remarks
 * **Types** control visual emphasis and intent:
 * - `primary` — High emphasis. The principal call-to-action in a workflow.
 * - `secondary` — Medium emphasis. Paired with Primary for alternative actions.
 * - `destructive` — Danger. Reserved for irreversible actions such as deletion.
 * - `menu` — A CTA with multiple options but no clear default. Triggers a dropdown Menu.
 *
 * **Variations** control size and content layout:
 * - `standard` — Default 40px height, 16/24 typography.
 * - `small` — Compact 34px height, 12/20 typography.
 * - Left icon — Pair an icon with text for CRUD actions (Add, Delete, Download).
 * - Icon-only — For space-constrained contexts; always provide an `aria-label`.
 *
 * **Visibility guidance:**
 * - Hide the button when the user lacks RBAC access and the cloud is disconnected.
 * - Disable (don't hide) when the user has RBAC access but the cloud is disconnected,
 *   so the user is aware of the connection issue.
 */
export declare const Button: React.ForwardRefExoticComponent<ButtonProps & React.RefAttributes<HTMLButtonElement>>;
//# sourceMappingURL=Button.d.ts.map