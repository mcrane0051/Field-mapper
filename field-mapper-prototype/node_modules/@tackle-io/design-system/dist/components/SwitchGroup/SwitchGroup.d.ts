import React from 'react';
export interface SwitchGroupProps {
    /** Group label text. */
    label?: string;
    /** Helper text displayed below the group. */
    helperText?: string;
    /** Error message — activates error state on group label and helper text. */
    error?: string;
    /** Layout direction. */
    direction?: 'vertical' | 'horizontal';
    /** Number of columns (for horizontal layout). */
    columns?: 1 | 2 | 3;
    /** Whether the group is required. */
    required?: boolean;
    /** Whether all switches in the group are disabled. */
    disabled?: boolean;
    /** Children switch components. */
    children: React.ReactNode;
    /** Additional className. */
    className?: string;
}
/**
 * A group of labeled switches forming a list of controls. A group header
 * or helper text may be included for additional user guidance.
 *
 * **Types**
 * - **Vertical** — Switches stacked in a single column.
 * - **Horizontal** — Switches laid out in rows.
 *
 * **Columns / Rows**
 * - One, Two, or Three columns (horizontal) or column groups (vertical).
 *
 * **Variations**
 * - Simple — Switch options only.
 * - With group label — Adds a `GroupLabel` above the options.
 * - With helper text — Adds a `HelperText` below the options.
 * - With group error — Error state with red `GroupLabel` and error `HelperText`.
 *
 * **Accessibility**
 * Wraps switches in a `<fieldset>` with `role="group"`. Uses `<legend>`
 * for the group label and `aria-describedby` for helper/error text.
 *
 * @see Switch
 * @see GroupLabel
 * @see HelperText
 */
export declare const SwitchGroup: React.FC<SwitchGroupProps>;
//# sourceMappingURL=SwitchGroup.d.ts.map