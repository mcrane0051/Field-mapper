import React from 'react';
export interface SkipLinkProps {
    /** The id of the target element to skip to (without the leading #). Defaults to "main-content". */
    targetId?: string;
    /** The visible link text. Defaults to "Skip to main content". */
    children?: React.ReactNode;
    /** Additional CSS class */
    className?: string;
}
/**
 * SkipLink — a visually hidden link that becomes visible on keyboard focus,
 * allowing users to bypass repeated navigation blocks and jump directly to
 * the main content area. Required by WCAG 2.4.1 (Bypass Blocks).
 *
 * Place as the very first focusable element in the page (before any
 * navigation or top bar). The target element must have a matching `id`.
 *
 * The `ApplicationShell` renders `<main>` for its content area; set
 * `id="main-content"` on it (or use a custom `targetId`).
 */
export declare const SkipLink: React.FC<SkipLinkProps>;
//# sourceMappingURL=SkipLink.d.ts.map