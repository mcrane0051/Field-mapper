import React from 'react';
export interface SwitchProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'type' | 'size'> {
    /** Label text for the switch */
    label?: string;
    /** Whether the switch is checked */
    checked?: boolean;
    /** Callback when switch state changes */
    onChange?: (event: React.ChangeEvent<HTMLInputElement>) => void;
    /** Whether the switch is disabled */
    disabled?: boolean;
}
/**
 * A toggle-like element that controls binary options (On/Off, True/False)
 * in real time.
 *
 * **Key takeaways**
 * - Use to activate/deactivate or enable/disable something in a macro or
 *   micro flow.
 * - For accessibility, pair with clear labels and/or helper text describing
 *   what the switch controls when on or off.
 * - Switches can be organized into groups or indicate a single decision.
 *
 * **States**
 * Default, Hover, Focus, Disabled.
 *
 * **Variations**
 * - On / Off.
 * - Labeled / Unlabeled.
 *
 * @see Checkbox — for multi-selection.
 * @see Radio — for single selection from a list.
 * @see SwitchGroup — for grouping switches with a label and helper text.
 */
export declare const Switch: React.ForwardRefExoticComponent<SwitchProps & React.RefAttributes<HTMLInputElement>>;
//# sourceMappingURL=Switch.d.ts.map