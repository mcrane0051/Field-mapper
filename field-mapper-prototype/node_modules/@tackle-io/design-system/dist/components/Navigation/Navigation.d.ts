import React from 'react';
export interface NavigationItem {
    /** Unique identifier for the navigation item */
    id: string;
    /** The icon component to display (ignored for dividers) */
    icon: React.ReactNode;
    /** The label text for the navigation item (ignored for dividers) */
    label: string;
    /** Whether this item is a divider separator */
    divider?: boolean;
    /** Click handler for the navigation item */
    onClick?: () => void;
}
export interface NavigationProps {
    /** Array of navigation items to display */
    items?: NavigationItem[];
    /** ID of the currently active navigation item */
    activeItemId?: string;
    /** Whether the navigation is collapsed */
    collapsed?: boolean;
    /** Callback when collapse toggle is clicked */
    onToggle?: () => void;
    /**
     * Logo content (e.g., image, text, or component). Defaults to {@link TackleLogo}
     * (plain white wordmark). Pass `null` to hide the logo area.
     */
    logo?: React.ReactNode;
    /** Account name for account switcher */
    accountName?: string;
    /** Label for account switcher (e.g., 'Switch accounts') */
    accountLabel?: string;
    /** Account avatar (optional) */
    accountAvatar?: React.ReactNode;
    /** Callback when account switcher is clicked */
    onAccountClick?: () => void;
    /** Environment name for environment switcher */
    environmentName?: string;
    /** Label for environment switcher (e.g., 'Switch environments') */
    environmentLabel?: string;
    /** Environment avatar element (e.g. square initial block). */
    environmentAvatar?: React.ReactNode;
    /**
     * Environment badge rendered between the name and secondary label
     * (e.g. a green Tag "Production"). Also shown as the compact collapsed
     * content when the sidebar is collapsed.
     */
    environmentBadge?: React.ReactNode;
    /**
     * Compact content shown instead of the avatar when the environment
     * footer is collapsed (e.g. a short Tag "Prod"). Defaults to
     * `environmentBadge` when not provided.
     */
    environmentCollapsedContent?: React.ReactNode;
    /** Callback when environment switcher is clicked */
    onEnvironmentClick?: () => void;
    /** User's display name */
    userName?: string;
    /** User's email address */
    userEmail?: string;
    /** User avatar (optional, defaults to UserCircledIcon) */
    userAvatar?: React.ReactNode;
    /** Callback when user footer is clicked */
    onUserClick?: () => void;
    /** Additional CSS class */
    className?: string;
}
/**
 * Full-height sidebar navigation composing NavHeader, NavMenuItem, and
 * NavFooter into the canonical Tackle "PrimaryNav" layout.
 *
 * **Anatomy**
 * - **NavHeader** — Logo + collapse toggle at the top.
 * - **NavList** — Vertical stack of `NavMenuItem` items (including divider
 *   separators), filling the remaining space.
 * - **Footer** — Up to three stacked `NavFooter` sections at the bottom:
 *   1. Account switcher (optional, shown when `accountName` is provided).
 *   2. Environment switcher (optional, shown when `environmentName` is provided).
 *   3. User information (always rendered).
 *
 * **Variations**
 * - **Expanded** — 240 px wide, labels visible.
 * - **Collapsed** — 64 px wide, icon-only view.
 *
 * **States** (per NavMenuItem)
 * - **Inactive (default)** — Teal 90 background.
 * - **Hover** — Teal 100 background.
 * - **Active** — Teal 100 background, 4 px Yellow 50 left indicator, bold label.
 *
 * **Accessibility**
 * Wraps content in `<nav aria-label="Primary navigation">`. Active items
 * receive `aria-current="page"`. Dividers render with `role="separator"`.
 * The collapse toggle has an appropriate `aria-label`.
 *
 * @remarks
 * Corresponds to the "PrimaryNav" ComponentSet in the Tackle Design Library.
 * The `items` prop accepts a `divider` flag on any `NavigationItem` to render
 * a horizontal separator. Default items match the canonical Figma nav order.
 */
export declare const Navigation: React.FC<NavigationProps>;
//# sourceMappingURL=Navigation.d.ts.map