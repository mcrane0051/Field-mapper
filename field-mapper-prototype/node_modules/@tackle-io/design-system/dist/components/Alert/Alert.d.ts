import React from 'react';
import './Alert.css';
export interface AlertProps {
    /**
     * Alert type/severity
     * @default 'info'
     */
    variant?: 'success' | 'info' | 'warning' | 'error';
    /**
     * Alert title/message
     */
    title: string;
    /**
     * Optional description text (enables multiline mode)
     */
    description?: string;
    /**
     * Icon to display (if not provided, default icon for variant is used)
     */
    icon?: React.ReactNode;
    /**
     * Whether the alert can be dismissed
     * @default false
     */
    dismissible?: boolean;
    /**
     * Callback when alert is dismissed
     */
    onDismiss?: () => void;
    /**
     * Optional action link text
     */
    actionText?: string;
    /**
     * Callback when action link is clicked
     */
    onAction?: () => void;
    /**
     * Required action button text (shows a button instead of link)
     */
    requiredActionText?: string;
    /**
     * Callback when required action button is clicked
     */
    onRequiredAction?: () => void;
    /**
     * Auto-dismiss after specified milliseconds (timed mode)
     */
    autoDismissMs?: number;
    /**
     * Additional CSS class names
     */
    className?: string;
    /**
     * ARIA role
     * @default 'alert'
     */
    role?: 'alert' | 'status';
}
/**
 * Alert — a notification that informs users of system updates and provides
 * real-time feedback on user actions. See also: Banner.
 *
 * @remarks
 * Alerts are generally placed at the top of the page (informational ones may
 * appear at the bottom). They always overlay page content and may include
 * calls to action. Include proper headings to preface relevant details in the
 * body as needed.
 *
 * **Types** control color and semantic intent:
 * - `success` — Positive confirmation (Green 70, white text).
 * - `info` — Informational / neutral (White bg, dark text).
 * - `warning` — Caution (Yellow 40, dark text).
 * - `error` — Danger / failure (Red 70, white text).
 *
 * **Interactions:**
 * - Timed — Auto-dismiss with progress circle (informational loading/progress).
 * - Dismissable — User closes via the X button.
 * - Optional action — Includes an underlined action link.
 * - Required action — Includes an underlined action link the user must act on.
 * - Required acceptance — Includes an "Acknowledge" button; dismisses on click.
 *
 * **Multiline** variants apply only to persistent alerts so content may be
 * read in full.
 */
export declare const Alert: React.FC<AlertProps>;
//# sourceMappingURL=Alert.d.ts.map