import React from 'react';
export interface RadioProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'type' | 'size'> {
    /** Label text for the radio */
    label?: string;
    /** Value of the radio button */
    value: string;
    /** Whether the radio is checked */
    checked?: boolean;
    /** Callback when radio state changes */
    onChange?: (event: React.ChangeEvent<HTMLInputElement>) => void;
    /** Whether the radio is disabled */
    disabled?: boolean;
    /** Name attribute for radio group */
    name?: string;
}
/**
 * A control used to make a single selection within a list, often applied
 * through a separate Submit action.
 *
 * Uses `RadioOnIcon` and `RadioOffIcon` from the icon library.
 *
 * **Key takeaways**
 * - Radio options should always default to a selection.
 * - Only one radio button can be selected at a time (not multiple).
 * - Each radio selection should have its own item and label, including
 *   help text as needed.
 *
 * **States**
 * Default, Hover, Focus, Disabled.
 *
 * **Variations**
 * - Empty / Filled (unchecked / checked).
 * - Labeled / Unlabeled.
 *
 * @see Checkbox — for multi-selection.
 * @see Switch — for immediate on/off toggles.
 * @see RadioGroup — for grouping radios with a label and helper text.
 */
export declare const Radio: React.ForwardRefExoticComponent<RadioProps & React.RefAttributes<HTMLInputElement>>;
//# sourceMappingURL=Radio.d.ts.map