import React from 'react';
export interface ModalHeaderProps {
    /** The title text of the modal. */
    title: string;
    /** Optional secondary description text below the title. */
    description?: string;
    /** Whether to show an icon next to the title. */
    showIcon?: boolean;
    /** Icon type to display when `showIcon` is true. */
    iconType?: 'info' | 'error';
    /** Callback when the close button is clicked. */
    onClose?: () => void;
    /** Additional CSS class. */
    className?: string;
    /**
     * Override the auto-generated `id` used for `aria-labelledby` on the title.
     * Useful when the parent modal needs to reference this element.
     */
    titleId?: string;
    /**
     * Override the auto-generated `id` used for `aria-describedby` on the description.
     * Useful when the parent modal needs to reference this element.
     */
    descriptionId?: string;
}
/**
 * ModalHeader — the header section shared by InfoModal, ActionModal, and
 * FormModal. Contains a title, optional description, optional leading icon,
 * and a close button.
 *
 * @remarks
 * **Variants:**
 * - `Icon=No` — Title and optional description only.
 * - `Icon=Yes` — Leading icon (info or error) beside the title; description
 *   indents to align with title text.
 *
 * **Dismissing / Closing:**
 * All modals need a visible way to close the overlay. Informational modals
 * use a close icon; actionable modals use a "Cancel" / "Okay" button.
 * Pressing Esc also closes the modal.
 *
 * **Accessibility:**
 * Focus must stay within the modal until dismissal. Title and description
 * elements carry unique `id` attributes so the parent `<dialog>` (or ARIA
 * role container) can reference them with `aria-labelledby` /
 * `aria-describedby`.
 */
export declare const ModalHeader: React.FC<ModalHeaderProps>;
//# sourceMappingURL=ModalHeader.d.ts.map