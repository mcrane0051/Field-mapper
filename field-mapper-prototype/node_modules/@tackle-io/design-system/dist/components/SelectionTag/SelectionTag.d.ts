import React, { ButtonHTMLAttributes } from 'react';
export interface SelectionTagProps extends Omit<ButtonHTMLAttributes<HTMLButtonElement>, 'children'> {
    /** The text content of the tag */
    children: React.ReactNode;
    /** Callback when the remove button is clicked. If not provided, tag is not removable */
    onRemove?: () => void;
    /** Whether the tag is disabled */
    disabled?: boolean;
    /** Additional CSS class */
    className?: string;
}
/**
 * SelectionTag — a multiselect pill used within the ComboBox component to
 * represent individually-selected values.
 *
 * @remarks
 * **Required prop** determines interactivity:
 * - `Required = No` (default when `onRemove` is provided) — renders as a
 *   `<button>` with a close icon; clicking removes the selection.
 * - `Required = Yes` (when `onRemove` is omitted) — renders as a static
 *   `<div>` with no close icon; the selection cannot be removed.
 *
 * **States:** Default, Hover, Focus, Disabled.
 *
 * For non-interactive categorical labels, see Tag.
 */
export declare const SelectionTag: React.ForwardRefExoticComponent<SelectionTagProps & React.RefAttributes<HTMLButtonElement>>;
//# sourceMappingURL=SelectionTag.d.ts.map