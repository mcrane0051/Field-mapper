import { SalesforceDrilldownShell, Button } from '@tackle-io/design-system';
import { NAV_ITEMS } from '../config/navigation';
import { QUICK_LINKS } from '../data/quickLinks';
import { FundingIcon } from '../icons/FundingIcon';
import { FormSectionCard } from '../components/FormSectionCard';
import { FormField } from '../components/FormField';

export function FundingRequestPage() {
  return (
    <SalesforceDrilldownShell
      navItems={NAV_ITEMS}
      activeNavItem="Benefits funding"
      breadcrumbs={[
        { label: "Acme Corp Summer '26", onClick: () => {} },
        { label: 'MDF Funding Request' }
      ]}
      pageTitle="MDF Funding Request"
      pageIcon={<FundingIcon />}
      quickLinks={QUICK_LINKS}
      actions={
        <>
          <Button variant="secondary">Cancel</Button>
          <Button variant="secondary">Save draft</Button>
          <Button variant="primary">Submit request</Button>
        </>
      }
    >
      <FormSectionCard id="vendor" title="Vendor Information">
        <FormField label="Vendor Name" value="Acme Corp" />
        <FormField label="Contact Email" value="partnerships@acmecorp.com" />
        <FormField label="Partner Tier" value="Premier" />
      </FormSectionCard>

      <FormSectionCard id="request-type" title="Request Type">
        <FormField label="Funding Category" value="Market Development Fund" />
        <FormField label="Campaign Type" value="Digital Marketing" />
      </FormSectionCard>

      <FormSectionCard id="activities" title="Activities">
        <FormField
          label="Activity Description"
          placeholder="Describe the planned activities..."
        />
        <FormField
          label="Expected Outcome"
          placeholder="Expected KPIs and outcomes..."
        />
      </FormSectionCard>

      <FormSectionCard id="budget" title="Budget Allocation">
        <FormField label="Requested Amount" value="$25,000" />
        <FormField label="Matching Funds" value="$25,000" />
        <FormField label="Total Budget" value="$50,000" />
      </FormSectionCard>

      <FormSectionCard id="attachments" title="Attachments">
        <div
          style={{
            padding: 16,
            textAlign: 'center',
            color: '#706e6b',
            fontSize: 13,
            border: '2px dashed #e5e5e5',
            borderRadius: 8
          }}
        >
          Drag files here or click to upload
        </div>
      </FormSectionCard>

      <FormSectionCard id="approvals" title="Approvals">
        <div style={{ fontSize: 13, color: '#706e6b' }}>
          No approval history yet. Submit the form to initiate the approval
          workflow.
        </div>
      </FormSectionCard>
    </SalesforceDrilldownShell>
  );
}
