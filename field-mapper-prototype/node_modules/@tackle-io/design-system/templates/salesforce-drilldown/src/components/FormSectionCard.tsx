import type { FC, ReactNode } from 'react';

export const FormSectionCard: FC<{
  id?: string;
  title: string;
  children: ReactNode;
}> = ({ id, title, children }) => (
  <div
    id={id}
    style={{
      background: '#fff',
      border: '1px solid #e5e5e5',
      borderRadius: 8,
      padding: '20px 24px',
      boxShadow: '0 1px 3px rgba(0,0,0,0.06)'
    }}
  >
    <h3
      style={{
        margin: '0 0 16px',
        fontSize: 15,
        fontWeight: 600,
        color: '#181818'
      }}
    >
      {title}
    </h3>
    {children}
  </div>
);
