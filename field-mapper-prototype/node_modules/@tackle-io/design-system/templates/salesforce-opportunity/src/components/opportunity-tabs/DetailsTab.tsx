import type { CSSProperties, FC } from 'react';

const fieldGrid: CSSProperties = {
  display: 'grid',
  gridTemplateColumns: '1fr 1fr',
  borderTop: '1px solid #e5e5e5'
};

const fieldCell: CSSProperties = {
  padding: '10px 16px',
  borderBottom: '1px solid #e5e5e5',
  fontSize: 13,
  color: '#181818'
};

const fieldLabel: CSSProperties = {
  fontSize: 12,
  color: '#3e3e3c',
  marginBottom: 2
};

const FieldPair: FC<{ label: string; value: string }> = ({ label, value }) => (
  <div style={fieldCell}>
    <div style={fieldLabel}>{label}</div>
    <div>{value}</div>
  </div>
);

const FUNDING_COLUMNS = [
  'Fund Request ID',
  'Template',
  'Program',
  'Activity Name',
  'Stage',
  'Status',
  'PO Number',
  'Budget Year',
  'Funding Expiration',
  'Opportunity'
];

const FundingRequestsSection = () => (
  <div style={{ marginTop: 24 }}>
    <div
      style={{
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'center',
        marginBottom: 12
      }}
    >
      <h3
        style={{ margin: 0, fontSize: 15, fontWeight: 700, color: '#181818' }}
      >
        Funding requests (preview)
      </h3>
      <button
        type="button"
        style={{
          padding: '6px 14px',
          borderRadius: 4,
          border: '1px solid #c9c9c9',
          background: '#fff',
          fontSize: 13,
          fontWeight: 600,
          color: '#181818',
          cursor: 'pointer'
        }}
      >
        Download sample CSV
      </button>
    </div>

    <div style={{ marginBottom: 12 }}>
      <label
        style={{
          display: 'block',
          fontSize: 12,
          fontWeight: 600,
          color: '#3e3e3c',
          marginBottom: 4
        }}
      >
        Search
      </label>
      <input
        type="text"
        placeholder="Search by activity, program, request ID..."
        style={{
          width: '50%',
          boxSizing: 'border-box',
          padding: '8px 12px',
          border: '1px solid #c9c9c9',
          borderRadius: 4,
          fontSize: 13,
          color: '#181818',
          background: '#fff'
        }}
      />
    </div>

    <div style={{ overflowX: 'auto' }}>
      <table
        style={{
          width: '100%',
          borderCollapse: 'collapse',
          fontSize: 13,
          color: '#181818'
        }}
      >
        <thead>
          <tr>
            {FUNDING_COLUMNS.map((col) => (
              <th
                key={col}
                style={{
                  textAlign: 'left',
                  padding: '8px 10px',
                  borderBottom: '2px solid #e5e5e5',
                  fontSize: 12,
                  fontWeight: 600,
                  color: '#3e3e3c',
                  whiteSpace: 'nowrap'
                }}
              >
                {col}
              </th>
            ))}
          </tr>
        </thead>
        <tbody>
          <tr>
            <td
              colSpan={FUNDING_COLUMNS.length}
              style={{
                padding: '16px 10px',
                textAlign: 'left',
                color: '#706e6b',
                borderBottom: '1px solid #e5e5e5'
              }}
            >
              No funding requests yet for this opportunity.
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
);

export function DetailsTab() {
  return (
    <div>
      <div style={fieldGrid}>
        <FieldPair label="Opportunity owner" value="Jason Dewar" />
        <FieldPair label="Private" value="not checked" />
        <FieldPair label="Amount" value="$120,000.00" />
        <FieldPair label="Close date" value="12/15/2026" />
        <FieldPair label="Stage" value="Id. Decision Makers" />
        <FieldPair label="Probability (%)" value="60%" />
        <FieldPair label="Account name" value="Acme Corp" />
        <FieldPair label="Type" value="New Business" />
      </div>
      <FundingRequestsSection />
    </div>
  );
}
