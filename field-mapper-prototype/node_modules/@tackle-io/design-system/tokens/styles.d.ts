/**
 * Typings for the `@tackle-io/design-system/styles` entry (CSS custom properties).
 * The implementation is the published bundle CSS (`dist/index.css`, tokens + components).
 * This file exists so TypeScript accepts the import.
 */
export {};
